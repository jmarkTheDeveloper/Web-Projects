# EcoCircuit Backend (Prototype)

Run the backend locally (requires Maven & JDK):

```bash
cd backend
mvn spring-boot:run
```

This exposes a minimal session API at `http://localhost:8080/api/session` supporting `GET`, `POST`, and `DELETE`.
